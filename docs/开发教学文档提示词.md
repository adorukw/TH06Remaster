# 需求：生成《C+++SDL2从零重置(Remaster)东方红魔乡》完整Markdown开发教程文档
## 基础约束
1. 开发技术栈：C++17 + SDL2 + SDL2_image + SDL2_mixer + CMake + Lua，在Linux平台开发为主，兼顾跨平台说明；
2. 项目用途：仅限个人本地学习研究，严禁商用、分发原版提取资源，文档开头明确版权红线；
3. 复刻标准：严格对标TH06原版行为、数值、渲染效果、弹幕轨迹、手感，做到1:1逻辑还原；
4. 文档格式：标准Markdown，多级标题分层，代码块使用C++/shell语法高亮，结构由前置→底层→业务→优化依次递进。

## 文档强制包含全部内容模块，每个模块需展开内部完整实现细节
### 第一部分：从零前置准备工作
1. 开发环境完整配置教程：VSCode、C++17 + SDL2 + SDL2_image + SDL2_mixer + CMake + Lua部署、依赖库引入、编译参数、调试模式配置
2. 素材完整获取流程：首先寻找已公开分享的全套TH06资源（最好是高清资源）,找不到之后再进行解包正版红魔乡前置要求、thtk全套工具使用、批处理一键解包th06.dat/紅魔郷MD.dat、ANM图集/ecl弹幕脚本/BGM音效/字体素材导出操作、命令示例
3. 素材标准化处理规范：
   - 图像：RGBA8888 PNG透明修复、图集合并、动画帧坐标元数据记录
   - 音频：44100Hz 16bit WAV标准化、BGM循环起止采样点记录、音效音量统一

### 第二部分：项目架构与代码文件规范
1. 标准分层式文件夹完整目录树（core底层/menu菜单/game逻辑/save存档/reference参考素材等）
2. C++代码设计规范：面向对象封装规则、全局资源管理规范、搭配Lua的硬编码规避方案
3. 资源配置文件规范：JSON/文本配置存储弹道、血量、动画、难度数值

### 第三部分：底层通用核心框架（必须附带完整可运行代码模板）
1. SDL2窗口与渲染封装：硬件加速、VSync、NEAREST像素缩放、分层渲染队列、Alpha混合模式配置
2. 固定60帧分离主循环实现（逻辑帧与渲染帧解耦，解决高刷屏超速问题）
3. 全局资源管理系统：纹理对象池、音频缓存、资源懒加载/释放机制
4. 输入系统：事件处理+实时键盘轮询双模式、射击长按连发、炸弹瞬时触发、菜单导航逻辑、自定义按键存档绑定
5. 红魔乡原生自定义RNG随机算法完整C++实现，用于弹幕、道具、随机行为复刻

### 第四部分：游戏核心业务系统（每个系统拆解底层实现细节、原版还原要点）
1. 玩家机体系统
   - 移动物理：无惯性瞬时变速、Shift低速倍率、屏幕边界限制
   - 射击系统：灵梦/魔理沙双武器弹道、发射间隔、子弹属性
   - 炸弹系统：全屏清弹、无敌时长、伤害判定、消耗机制、专属动画音效
   - 碰撞判定：中心极小圆形玩家判定、中弹无敌帧、复活无敌规则
2. 弹幕系统（STG核心，重点细化）
   - 对象池预分配实现，规避频繁new/delete性能损耗
   - 弹幕基础属性：位置浮点、速度、角速度、加速度、存活帧、碰撞半径
   - 原版全类型弹道算法：直线、正弦摆动、螺旋、追踪、环形扩散等
   - 批量同纹理渲染优化方案、弹幕生命周期销毁规则、屏幕边界剔除逻辑
   - 弹幕与玩家/玩家子弹双向圆形碰撞检测
3. 敌人、BOSS与符卡系统
   - 普通杂兵：固定移动路径、定时弹幕生成、血量、死亡爆炸、道具掉落
   - BOSS调度机制：多阶段血量阈值、阶段切换动画、移动路径规则
   - 符卡Spell Card完整生命周期：符卡开启弹窗、倒计时、限时得分、符卡结束清弹幕、符卡奖励倍率、符卡失败判定
4. 道具系统
   - 全类型道具：分数、Power火力、残机碎片、炸弹、生命道具
   - 道具自动上浮逻辑、玩家吸附范围、Shift低速扩大吸附机制、道具出界销毁、拾取加分计算
5. 多层卷轴视差背景系统
   - 多图层独立滚动坐标管理、每层滚动速度配置
   - 关卡推进全局偏移、BOSS战背景减速/停止逻辑、背景分层绘制顺序
6. UI与像素文字渲染系统
   - 禁用TTF矢量字体，基于原版sft图集自制位图文字渲染器
   - 战斗常驻UI：分数、残机、炸弹、火力、关卡标识
   - 动态UI：得分飘字、符卡名称弹窗、警告文字、Game Over/通关界面、全功能菜单渲染
7. 存档系统
   - 二进制本地存档文件设计、最高分存储、自定义按键配置、难度/关卡解锁数据读写
8. 分数计算系统
   - 基础道具得分、符卡限时奖励倍率、残机剩余加分、连击分数规则、总分存储逻辑
9. Replay录像回放系统（东方标志性功能）
   - 录制逻辑：每帧键盘输入缓存、开局RNG种子保存
   - 回放逻辑：重放输入、复现全部随机弹幕行为、录像文件读写结构
10. 难度控制系统
    - Easy/Normal/Hard/Lunatic四难度差异化配置项：敌人血量、弹幕速度、弹幕密度、符卡时限、道具掉落概率
    - 全局难度参数统一管理，一键切换
11. 全局游戏流程控制器
    - 状态机实现：主菜单→难度选择→角色选择→关卡加载→BOSS战→通关/GameOver→返回菜单
    - 关卡波次调度、敌人生成计时器、场景切换过渡逻辑

### 第五部分：性能优化、调试工具、复刻避坑指南
1. SDL2性能优化方案：万级弹幕批渲染、纹理缓存复用、音频声道限制、内存释放管理
2. 内置调试工具实现：碰撞盒可视化绘制、单步帧前进、游戏暂停、实时帧率/弹幕数量打印
3. 高频复刻踩坑完整清单：帧率超速、透明贴图发黑、BGM循环断音、录像弹幕错位、输入延迟、像素画面模糊等问题根源与修复方案
4. 可选拓展功能开发指引：游戏截图、自定义窗口缩放、慢动作调试模式

## 文档额外要求
1. 每个核心模块标注「原版1:1还原关键细节」，明确复刻不能省略的逻辑；
2. 代码示例简洁可直接集成进项目，注释清晰；
3. 素材相关章节提供完整可运行bat批处理、命令行指令；
4. 区分开发阶段顺序，给出从0到可运行游戏的分步开发流程；
5. 结尾补充拓展方向：移植Windows/mac、自制原创素材、新增自定义关卡编辑器。